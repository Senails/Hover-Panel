import type { Meta, StoryObj } from '@storybook/react';
import { HexagonSlot } from '.';


const meta: Meta<typeof HexagonSlot> = {
    title: 'MyComponents/HexagonSlot',
    component: HexagonSlot
};

export default meta;
type Story = StoryObj<typeof HexagonSlot>;

export const Primary: Story = {
};


