import type { Meta, StoryObj } from '@storybook/react';
import { HoverPanel } from '.';


const meta: Meta<typeof HoverPanel> = {
    title: 'MyComponents/HoverPanel',
    component: HoverPanel
};

export default meta;
type Story = StoryObj<typeof HoverPanel>;

export const Primary: Story = {};


